schema {
 query: Query
}

type Query {
 allPlayers: [Player]
 player(id: String): Player
}

type Player {
  id: String
  name: String
  country: String
  clubsPlayed: [String]
  dateOfBirth: String
  age: String
}